#!/bin/sh

filename=$(date +'%F_%H-%M-%S')

ffmpeg -video_size 1920x1080 -framerate 60 -r 30 -f x11grab -i "$DISPLAY" -f pulse -i default -vcodec libx264 -acodec libopus -crf 0 -preset ultrafast ~/Videos/youtube/"$filename".mkv
